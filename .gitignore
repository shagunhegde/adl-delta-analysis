# Regenerated by the pipeline; never committed.
artifacts/          # a fresh run writes here; the archived reference copies are in expected/
students/           # ~92 MB of LoRA weights per student
data/*              # generated corpora (spike-ins) — deterministic given the seeds
!data/neutral_numbers.jsonl   # except this one: it is not downloadable from HF
out/
diffing-toolkit/    # cloned by scripts/reproduce.sh at the pinned commit
reproduction/

# Never commit the grader key
openrouter_api_key.txt
*.env.local

__pycache__/
*.pyc
.venv*/
.DS_Store
